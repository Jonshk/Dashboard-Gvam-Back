# — PostgreSQL ya existente —
DB_HOST=postgres
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=dashboard

# — Variables para JWT (imprescindibles) —
JWT_SECRET=tu_super_secreto
JWT_ALGORITHM=HS256
JWT_EXP_DELTA_SECONDS=3600
